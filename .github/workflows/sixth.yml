name: Email Workflow Demo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 23f1001883@ds.study.iitm.ac.in
        run: echo "Hello, world! This step includes my email."

      - name: Verify run
        run: echo "GitHub Action executed successfully!"
